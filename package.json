{
  "name": "nmiai-tripletex-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "check": "tsc --noEmit",
    "test:solve": "node scripts/test-solve.mjs",
    "analyze:solve": "node scripts/analyze-failures.mjs",
    "autopilot:solve": "node scripts/autopilot-solve.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "fastify": "^5.6.1",
    "pino-pretty": "^13.1.2"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
